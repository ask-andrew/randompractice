{
  "name": "sax-practice-tool",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://ask-andrew.github.io/randompractice/",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "abcjs": "^6.2.0"
  },
  "scripts": {
    "start": "react-scripts start",   <-- MODIFIED
    "build": "react-scripts build",   <-- MODIFIED
    "test": "react-scripts test",     <-- MODIFIED (if you don't have react-scripts test, just use "echo \"Run tests\"")
    "predeploy": "npm run build",     <-- ADDED
    "deploy": "gh-pages -d build"     <-- ADDED
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
